const path = require('path');

console.log('Стрічка котру повертає __dirname -> ' + path.resolve(__dirname));
console.log('Стрічка котру повертає __filename -> ' + path.resolve(__filename));

// Прописати команду node index.js в терміналі
// Через термінал знаходитись в папці де знаходиться це файл!
// Розібратись як пересуватись між папками через термінал:
// команда cd, автозаповнення шляхів, ../